interface UserPreview {
  id: string;
  title: string;
  firstName: string;
  lastName: string;
  picture: string;
}

interface UserFull {
  id: string;
  title: string;
  firstName: string;
  lastName: string;
}

export { UserPreview, UserFull };
